C++11 和 C++14 引入了许多优秀的功能：`auto`、智能指针、移动语义、lambda 和并发。这些功能都会在单独的章节中详细介绍。对于追求效率的现代 C++ 程序员而言，还需要采取一系列更小的步骤，才能从 C++98 过渡到现代 C++。例如，什么时候应使用 `{}` 而不是 `()` 创建对象？为什么类型别名比 `typedef` 更好？`constexpr` 与 `const` 有何不同？`const` 成员函数与线程安全有什么关系？本章将一一解答这些问题。
