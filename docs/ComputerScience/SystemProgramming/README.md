直接调用操作系统 API（`application programming interface`）所提供的服务的程序称为系统程序（`system program`）。在编写这类程序时就是在进行系统编程（`system programming`）。系统程序（`system program`）这个术语有的时候也适用于独立于操作系统运行并扩展其功能的程序，例如编译器、汇编器、链接器、终端模拟器等等，它们扮演了计算机系统中的基本角色，可以看作是操作系统的扩展。

这里讨论如何编写这些系统程序，即能够与操作系统直接交互并且实际上充当了系统的一部分的程序。
